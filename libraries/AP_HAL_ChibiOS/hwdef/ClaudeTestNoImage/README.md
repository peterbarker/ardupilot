# ClaudeTestNoImage

A test board that deliberately omits any board image.

This README contains no markdown image reference, so the new-board image
requirement enforced by `Tools/scripts/test_new_boards.py` should fail for
this board.

name: test size

on:
  pull_request:
    paths-ignore:  # ignore autotest stuffs
      - 'Tools/autotest/**'
      # Remove markdown files as irrelevant
      - '**.md'
      # Remove dotfile at root directory
      - './.dir-locals.el'
      - './.dockerignore'
      - './.editorconfig'
      - './.flake8'
      - './.gitattributes'
      - './.github'
      - './.gitignore'
      - './.pre-commit-config.yaml'
      - './.pydevproject'
      - './.valgrind-suppressions'
      - './.valgrindrc'
      - 'Dockerfile'
      - 'Vagrantfile'
      - 'Makefile'
      # Remove some directories check
      - '.vscode/**'
      - '.github/ISSUE_TEMPLATE/**'
      # Remove generic tools
      - 'Tools/CHDK-Script/**'
      - 'Tools/CodeStyle/**'
      - 'Tools/completion/**'
      - 'Tools/CPUInfo/**'
      - 'Tools/debug/**'
      - 'Tools/environment_install/**'
      - 'Tools/FilterTestTool/**'
      - 'Tools/geotag/**'
      - 'Tools/GIT_Test/**'
      - 'Tools/gittools/**'
      - 'Tools/Hello/**'
      - 'Tools/Linux_HAL_Essentials/**'
      - 'Tools/mavproxy_modules/**'
      - 'Tools/Pozyx/**'
      - 'Tools/PrintVersion.py'
      - 'Tools/simulink/**'
      - 'Tools/UDP_Proxy/**'
      - 'Tools/vagrant/**'
      - 'Tools/Vicon/**'
      # remove non CHIBIOS HAL
      - 'libraries/AP_HAL_SITL/**'
      - 'libraries/AP_HAL_ESP32/**'
      - 'libraries/AP_HAL_Linux/**'
      # Remove change on other workflows
      - '.github/workflows/test_environment.yml'
  workflow_dispatch:


concurrency:
  group: ci-${{github.workflow}}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-22.04
    container: ardupilot/ardupilot-dev-chibios:v0.1.3

    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
          submodules: 'recursive'

      # Put ccache into github cache for faster build
      - name: Prepare ccache timestamp
        id: ccache_cache_timestamp
        run: |
          NOW=$(date -u +"%F-%T")
          echo "timestamp=${NOW}" >> $GITHUB_OUTPUT

      - name: ccache cache files
        uses: actions/cache@v4
        with:
          path: ~/.ccache
          key: ${{github.workflow}}-ccache-${{steps.ccache_cache_timestamp.outputs.timestamp}}
          restore-keys: ${{github.workflow}}-ccache-

      - name: setup ccache
        run: |
          . .github/workflows/ccache.env

      - name: Setup git references for size comparison
        shell: bash
        run: |
          git config --global --add safe.directory ${GITHUB_WORKSPACE}
          # Add the base repository as a remote and fetch the base branch
          git remote add upstream ${{ github.event.pull_request.base.repo.clone_url }}
          git fetch --no-tags --prune --progress upstream ${{ github.event.pull_request.base.ref }}

      - name: Build and compare sizes using size_compare_branches.py
        shell: bash
        run: |
          PATH="/github/home/.local/bin:$PATH"

          # Run size_compare_branches.py with parallelization
          # Use upstream/ref instead of just ref since that's where we fetched it
          python3 Tools/scripts/size_compare_branches.py \
            --board Durandal \
            --board MatekF405 \
            --board KakuteF7 \
            --board MatekH743-bdshot \
            --board MambaH743v4 \
            --board Pixhawk1-1M \
            --board MatekF405-CAN \
            --board DrotekP3Pro \
            --board Hitec-Airspeed \
            --board f103-GPS \
            --all-vehicles \
            --features \
            --parallel-copies 4 \
            -j 2 \
            --master-branch upstream/${{ github.event.pull_request.base.ref }}
